# Setup artefacts — never commit these
.setup-config.json
setup.lock
*.bak
*.log

# Per-machine Claude Code settings (permissions, etc.) — local to each clone
.claude/

# Per-machine VS Code settings (launch configs, etc.) — local to each clone
.vscode/
# Packaged scripts contain embedded AES-256 SMTP credentials — never commit these
*_pkg.ps1
