# SmallScripts

Smaller, single-purpose scripts that do one specific job. Unlike the main scripts they are not served via the library web server — they are meant to be downloaded and run directly, or deployed via Intune / RMM.

